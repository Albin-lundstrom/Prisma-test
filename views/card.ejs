<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
        <p><%= data[i].id %></p>
        <p><%= data[i].email %></p>
        <p><%= data[i].phone %></p>
        <% if(data[i].img != null){ %>
            <img src="/img/<%= data[i].img %>" alt="img">
        <% }; %>
</body>
</html>